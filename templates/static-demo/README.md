# Static demo template

Use this directory as the starting layout for a new static demo.

Each published demo should contain:

- `index.html`
- Project-specific styles and scripts
- Local presentation-safe assets
- Relative URLs compatible with `/CCDemos/<project-name>/`

Copy the template into a new top-level project folder before adding it to the public demo hub.
