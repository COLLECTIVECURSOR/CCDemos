# Collective Cursor demo

Place presentation-safe Collective Cursor website concepts in this folder.

The entry point must be `index.html`, and all local assets should use relative paths so the demo works at `/CCDemos/collective-cursor/`.
