# LeadCrest demo

Place the presentation-safe LeadCrest static site in this folder.

The entry point must be `index.html`, and all local assets should use relative paths so the demo works at `/CCDemos/leadcrest/`.
